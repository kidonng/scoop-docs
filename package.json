{
  "private": true,
  "scripts": {
    "dev": "vuepress dev site",
    "build": "vuepress build site",
    "index": "node scripts/indexApps.js",
    "now-build": "yarn build"
  },
  "devDependencies": {
    "prettier": "^1.18.2"
  },
  "engines": {
    "node": "10.x"
  },
  "workspaces": [
    "site",
    "scripts"
  ],
  "prettier": {
    "semi": false,
    "singleQuote": true
  }
}
