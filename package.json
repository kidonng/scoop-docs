{
  "private": true,
  "scripts": {
    "dev": "vuepress dev packages/docs",
    "build": "vuepress build packages/docs",
    "index": "node packages/index/apps.js",
    "now-build": "yarn build"
  },
  "devDependencies": {
    "prettier": "^1.18.2"
  },
  "engines": {
    "node": "10.x"
  },
  "workspaces": [
    "packages/*"
  ],
  "prettier": {
    "semi": false,
    "singleQuote": true
  }
}
